runtime: "container"
startCommand:
  type: "stdio"
  command: "node"
  args: ["index.js"]
description: "Agent hiring marketplace with escrow. Post tasks, take bids, lock payment, release on completion. The job market for AI."
